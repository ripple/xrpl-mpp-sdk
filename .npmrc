registry=https://registry.npmjs.org/
# Publish with a Sigstore attestation tying the tarball to the CI run and commit
# that produced it. Requires a CI-only publish, which is still to be set up.
provenance=true
